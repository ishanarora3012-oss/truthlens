"""DistilBERT inference entry point placeholder."""


def predict(question: str, answer: str) -> float:
    """Predict hallucination probability with DistilBERT.

    TODO: load a versioned DistilBERT checkpoint and run batched inference.
    """
    del question, answer
    raise NotImplementedError
